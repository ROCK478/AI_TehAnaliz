# Ядро приложения (необходимый минимум для запуска)
Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Login>=0.6
requests>=2.31
email-validator>=2.1

# Опционально — нужно только для обучения настоящей ML-модели (train_model.py).
# Без них приложение работает на rule-based прогнозе (заглушке).
numpy>=1.26
scikit-learn>=1.4
joblib>=1.3
